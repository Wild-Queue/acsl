The Language acsl_grammar
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of acsl_grammar==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in acsl_grammar are the following:
  | ``admit`` | ``allocates`` | ``assert`` | ``assigns``
  | ``assumes`` | ``at`` | ``axiom`` | ``axiomatic``
  | ``behavior`` | ``behaviors`` | ``boolean`` | ``breaks``
  | ``char`` | ``check`` | ``complete`` | ``continues``
  | ``contract`` | ``decreases`` | ``disjoint`` | ``double``
  | ``ensures`` | ``exits`` | ``float`` | ``for``
  | ``frees`` | ``function`` | ``global`` | ``impact``
  | ``include`` | ``inductive`` | ``int`` | ``integer``
  | ``invariant`` | ``label`` | ``lemma`` | ``let``
  | ``logic`` | ``long`` | ``loop`` | ``model``
  | ``module`` | ``pragma`` | ``predicate`` | ``reads``
  | ``real`` | ``requires`` | ``returns`` | ``short``
  | ``signed`` | ``sizeof`` | ``slice`` | ``terminates``
  | ``type`` | ``unsigned`` | ``variant`` | ``void``
  | ``writes`` |  |  |

The symbols used in acsl_grammar are the following:
  | ; | \from | \nothing | ==>
  | <==> | || | && | ^^
  | & | | | ^ | -->
  | <--> | \in | ? | :
  | , | < | > | <=
  | >= | == | != | !
  | \forall | \exists | \lambda | *
  | [ | ] | ( | )
  | \true | \false | \object_pointer | \valid
  | \valid_read | \valid_function | \valid_index | \valid_range
  | \initialized | \dangling | \fresh | \base_addr
  | \block_length | \offset | \allocable | \freeable
  | \allocation | \automatic | \dynamic | \register
  | \static | \unallocated | \null | +
  | - | / | % | ~
  | \old | \at | \result | \separated
  | \pi | { | } | ..
  | _Bool |  |  |

===Comments===
There are no single-line comments in the grammar.There are no multiple-line comments in the grammar.

==The syntactic structure of acsl_grammar==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //Annot//
  |  |  **|**  | //LogicPTreeAnnot//
  | //Annot// | -> | //Code_Annot//
  | //Code_Annot// | -> | //Contract//
  | //Contract// | -> | //Requires// //Terminates// //Decreases// //SimpleClauses// //Behaviors// //CompleteOrDisjoint//
  |  |  **|**  | //Requires// //Terminates// //Decreases// //NESimpleClauses// ``requires``
  |  |  **|**  | //Requires// //Terminates// //Decreases// //NESimpleClauses// ``terminates``
  |  |  **|**  | //Requires// //Terminates// //Decreases// //NESimpleClauses// ``decreases``
  | //Requires// | -> | **eps**
  |  |  **|**  | //NERequires//
  | //NERequires// | -> | ``requires`` //Lexpr// ``;`` //Requires//
  |  |  **|**  | ``check`` ``requires`` //Lexpr// ``;`` //Requires//
  |  |  **|**  | ``admit`` ``requires`` //Lexpr// ``;`` //Requires//
  | //Terminates// | -> | **eps**
  | //Decreases// | -> | **eps**
  | //SimpleClauses// | -> | **eps**
  |  |  **|**  | //NESimpleClauses//
  | //NESimpleClauses// | -> | //PostCond// //Lexpr// ``;`` //SimpleClauses//
  |  |  **|**  | ``assigns`` //Assigns// ``;`` //SimpleClauses//
  | //Assigns// | -> | //Zones//
  |  |  **|**  | //[Lexpr]// ``\from`` //Zones//
  | //Zones// | -> | //[Lexpr]//
  |  |  **|**  | ``\nothing``
  | //Behaviors// | -> | **eps**
  | //CompleteOrDisjoint// | -> | **eps**
  | //Lexpr// | -> | //Lexpr1// ``==>`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``<==>`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``||`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``&&`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``^^`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``&`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``|`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``^`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``-->`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``<-->`` //Lexpr//
  |  |  **|**  | //Lexpr1// ``\in`` //Lexpr//
  |  |  **|**  | //Lexpr// ``?`` //Lexpr// ``:`` //Lexpr//
  |  |  **|**  | //Lexpr1//
  | //[Lexpr]// | -> | //Lexpr//
  |  |  **|**  | //Lexpr// ``,`` //[Lexpr]//
  | //Lexpr1// | -> | //Lexpr2//
  |  |  **|**  | //Lexpr3// //[LexprRelInner]// //Relation// //Lexpr2//
  | //LexprRelInner// | -> | //Relation// //Lexpr3//
  | //[LexprRelInner]// | -> | **eps**
  |  |  **|**  | //LexprRelInner// //[LexprRelInner]//
  | //Relation// | -> | ``<``
  |  |  **|**  | ``>``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``>=``
  |  |  **|**  | ``==``
  |  |  **|**  | ``!=``
  | //Lexpr2// | -> | //Lexpr3//
  |  |  **|**  | //LexprBinder//
  |  |  **|**  | ``!`` //LexprBinder//
  | //LexprBinder// | -> | ``\forall`` //Binders// ``;`` //Lexpr//
  |  |  **|**  | ``\exists`` //Binders// ``;`` //Lexpr//
  |  |  **|**  | ``\lambda`` //Binders// ``;`` //Lexpr//
  | //Binders// | -> | //TypeSpecOFTYPENAME// //VarSpec// //[BindersReentrance]//
  | //TypeSpecOFTYPENAME// | -> | //Ident//
  |  |  **|**  | //TypeSpecSimple//
  | //BindersReentrance// | -> | ``,`` //TypeSpecOFTYPENAME// //VarSpec//
  |  |  **|**  | ``,`` //VarSpec//
  | //[BindersReentrance]// | -> | **eps**
  |  |  **|**  | //BindersReentrance// //[BindersReentrance]//
  | //VarSpec// | -> | //VarSpec1//
  |  |  **|**  | //[Stars]// //VarSpec1//
  | //Stars// | -> | ``*``
  | //[Stars]// | -> | //Stars//
  |  |  **|**  | //Stars// //[Stars]//
  | //VarSpec1// | -> | //FullIdentifier//
  |  |  **|**  | //VarSpec1// ``[`` //ArraySize// ``]``
  |  |  **|**  | ``(`` //VarSpec// ``)``
  | //ArraySize// | -> | //Integer//
  |  |  **|**  | //FullIdentifier//
  |  |  **|**  | **eps**
  | //Lexpr3// | -> | ``\true``
  |  |  **|**  | ``\false``
  |  |  **|**  | ``\object_pointer`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\valid`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\valid_read`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\valid_function`` ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\valid_index`` //OptLabel1// ``(`` //Lexpr// ``,`` //Lexpr// ``)``
  |  |  **|**  | ``\valid_range`` //OptLabel1// ``(`` //Lexpr// ``,`` //Lexpr// ``,`` //Lexpr// ``)``
  |  |  **|**  | ``\initialized`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\dangling`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\fresh`` //OptLabel2// ``(`` //Lexpr// ``,`` //Lexpr// ``)``
  |  |  **|**  | ``\base_addr`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\block_length`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\offset`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\allocable`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\freeable`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\allocation`` //OptLabel1// ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\automatic``
  |  |  **|**  | ``\dynamic``
  |  |  **|**  | ``\register``
  |  |  **|**  | ``\static``
  |  |  **|**  | ``\unallocated``
  |  |  **|**  | ``\null``
  |  |  **|**  | //Integer//
  |  |  **|**  | //Double//
  |  |  **|**  | //String//
  |  |  **|**  | //Lexpr3// ``+`` //Lexpr3//
  |  |  **|**  | //Lexpr3// ``-`` //Lexpr3//
  |  |  **|**  | //Lexpr3// ``*`` //Lexpr3//
  |  |  **|**  | //Lexpr3// ``/`` //Lexpr3//
  |  |  **|**  | //Lexpr3// ``%`` //Lexpr3//
  |  |  **|**  | //Lexpr3// ``[`` //Range// ``]``
  |  |  **|**  | //Lexpr3// ``[`` //Lexpr// ``]``
  |  |  **|**  | ``-`` //Lexpr3//
  |  |  **|**  | ``+`` //Lexpr3//
  |  |  **|**  | ``~`` //Lexpr3//
  |  |  **|**  | ``*`` //Lexpr3//
  |  |  **|**  | ``&`` //Lexpr3//
  |  |  **|**  | ``sizeof`` ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\old`` ``(`` //Lexpr// ``)``
  |  |  **|**  | ``\at`` ``(`` //Lexpr// ``,`` //LabelName// ``)``
  |  |  **|**  | ``\result``
  |  |  **|**  | ``\separated`` ``(`` //[Lexpr]// ``)``
  |  |  **|**  | //FullIdentifier// ``(`` //[Lexpr]// ``)``
  |  |  **|**  | //FullIdentifier//
  |  |  **|**  | ``\pi``
  |  |  **|**  | ``(`` //Lexpr// ``)``
  |  |  **|**  | ``(`` //Range// ``)``
  | //OptLabel1// | -> | //OptLabelList//
  | //OptLabel2// | -> | //OptLabelList//
  | //OptLabelList// | -> | **eps**
  |  |  **|**  | ``{`` //[LabelName]// ``}``
  | //LabelName// | -> | //AnyIdentifier//
  | //[LabelName]// | -> | //LabelName//
  |  |  **|**  | //LabelName// ``,`` //[LabelName]//
  | //Range// | -> | //LexprOption// ``..`` //LexprOption//
  | //LexprOption// | -> | **eps**
  |  |  **|**  | //Lexpr//
  | //AnyIdentifier// | -> | //Identifier//
  | //PostCond// | -> | ``ensures``
  |  |  **|**  | ``exits``
  |  |  **|**  | ``breaks``
  |  |  **|**  | ``continues``
  |  |  **|**  | ``returns``
  |  |  **|**  | ``check`` ``ensures``
  |  |  **|**  | ``check`` ``exits``
  |  |  **|**  | ``check`` ``breaks``
  |  |  **|**  | ``check`` ``continues``
  |  |  **|**  | ``check`` ``returns``
  |  |  **|**  | ``admit`` ``ensures``
  |  |  **|**  | ``admit`` ``exits``
  |  |  **|**  | ``admit`` ``breaks``
  |  |  **|**  | ``admit`` ``continues``
  |  |  **|**  | ``admit`` ``returns``
  | //FullIdentifier// | -> | //Identifier//
  |  |  **|**  | ``admit``
  |  |  **|**  | ``allocates``
  |  |  **|**  | ``assert``
  |  |  **|**  | ``assigns``
  |  |  **|**  | ``assumes``
  |  |  **|**  | ``at``
  |  |  **|**  | ``axiom``
  |  |  **|**  | ``axiomatic``
  |  |  **|**  | ``behavior``
  |  |  **|**  | ``breaks``
  |  |  **|**  | ``check``
  |  |  **|**  | ``complete``
  |  |  **|**  | ``continues``
  |  |  **|**  | ``contract``
  |  |  **|**  | ``decreases``
  |  |  **|**  | ``disjoint``
  |  |  **|**  | ``ensures``
  |  |  **|**  | ``exits``
  |  |  **|**  | ``frees``
  |  |  **|**  | ``function``
  |  |  **|**  | ``global``
  |  |  **|**  | ``impact``
  |  |  **|**  | ``inductive``
  |  |  **|**  | ``include``
  |  |  **|**  | ``invariant``
  |  |  **|**  | ``lemma``
  |  |  **|**  | ``let``
  |  |  **|**  | ``logic``
  |  |  **|**  | ``loop``
  |  |  **|**  | ``model``
  |  |  **|**  | ``module``
  |  |  **|**  | ``pragma``
  |  |  **|**  | ``predicate``
  |  |  **|**  | ``requires``
  |  |  **|**  | ``returns``
  |  |  **|**  | ``slice``
  |  |  **|**  | ``terminates``
  |  |  **|**  | ``type``
  |  |  **|**  | ``variant``
  | //[FullIdentifier]// | -> | //FullIdentifier//
  |  |  **|**  | //FullIdentifier// ``,`` //[FullIdentifier]//
  | //Identifier// | -> | //Ident//
  |  |  **|**  | ``behaviors``
  |  |  **|**  | ``label``
  |  |  **|**  | ``reads``
  |  |  **|**  | ``writes``
  | //TypeSpecSimple// | -> | ``integer``
  |  |  **|**  | ``real``
  |  |  **|**  | ``boolean``
  |  |  **|**  | ``void``
  |  |  **|**  | ``_Bool``
  |  |  **|**  | ``char``
  |  |  **|**  | ``signed`` ``char``
  |  |  **|**  | ``unsigned`` ``char``
  |  |  **|**  | ``int``
  |  |  **|**  | ``signed`` ``int``
  |  |  **|**  | ``unsigned`` ``int``
  |  |  **|**  | ``unsigned``
  |  |  **|**  | ``short``
  |  |  **|**  | ``signed`` ``short``
  |  |  **|**  | ``unsigned`` ``short``
  |  |  **|**  | ``short`` ``int``
  |  |  **|**  | ``signed`` ``short`` ``int``
  |  |  **|**  | ``unsigned`` ``short`` ``int``
  |  |  **|**  | ``long``
  |  |  **|**  | ``signed`` ``long``
  |  |  **|**  | ``unsigned`` ``long``
  |  |  **|**  | ``signed`` ``long`` ``int``
  |  |  **|**  | ``long`` ``int``
  |  |  **|**  | ``unsigned`` ``long`` ``int``
  |  |  **|**  | ``long`` ``long``
  |  |  **|**  | ``signed`` ``long`` ``long``
  |  |  **|**  | ``unsigned`` ``long`` ``long``
  |  |  **|**  | ``long`` ``long`` ``int``
  |  |  **|**  | ``signed`` ``long`` ``long`` ``int``
  |  |  **|**  | ``unsigned`` ``long`` ``long`` ``int``
  |  |  **|**  | ``float``
  |  |  **|**  | ``double``
  |  |  **|**  | ``long`` ``double``
  | //LogicPTreeAnnot// | -> | //Annotation//
  | //Annotation// | -> | //LoopAnnotStack//
  | //LoopAnnotStack// | -> | //LoopInvariant// //LoopAnnotOpt//
  |  |  **|**  | //LoopEffects// //LoopAnnotOpt//
  |  |  **|**  | //LoopAllocation// //LoopAnnotOpt//
  |  |  **|**  | //LoopVariant// //LoopAnnotOpt//
  | //LoopAnnotOpt// | -> | **eps**
  |  |  **|**  | //LoopAnnotStack//
  | //LoopInvariant// | -> | ``loop`` ``invariant`` //Lexpr// ``;``
  |  |  **|**  | ``check`` ``loop`` ``invariant`` //Lexpr// ``;``
  |  |  **|**  | ``admit`` ``loop`` ``invariant`` //Lexpr// ``;``
  | //LoopAllocation// | -> | ``loop`` //Allocation// ``;``
  | //Allocation// | -> | ``allocates`` //Zones//
  |  |  **|**  | ``frees`` //Zones//
  | //LoopVariant// | -> | ``loop`` ``variant`` //Variant// ``;``
  | //LoopEffects// | -> | ``loop`` ``assigns`` //Assigns// ``;``
  | //Variant// | -> | //Lexpr// ``for`` //AnyIdentifier//
  |  |  **|**  | //Lexpr//



%% File generated by the BNF Converter (bnfc 2.9.4.1).
